# === horatad-media .gitignore ===
# เก็บ source Remotion (epicycle/src) — กัน node_modules, installer, วิดีโอ render (อยู่ Drive แล้ว)

# --- deps ---
node_modules/
**/node_modules/

# --- epicycle/ = source Remotion (รวมเข้า root repo แล้ว 15 มิ.ย. 2569 · เดิมเป็น repo แยกไม่มี remote → source ไม่ถูก backup) ---
# เก็บ epicycle/src + scripts + public(audio/vo) · กัน node_modules/out (global ด้านบนคุมแล้ว) + AI-tool skill dirs (machine-local junk)
epicycle/.agent/
epicycle/.agents/
epicycle/.claude/
epicycle/.cline/
epicycle/.codebuddy/
epicycle/.codex/
epicycle/.commandcode/
epicycle/.continue/
epicycle/.crush/
epicycle/.cursor/
epicycle/.factory/
epicycle/.gemini/
epicycle/.github/
epicycle/.goose/
epicycle/.junie/
epicycle/.kilocode/
epicycle/.kiro/
epicycle/.kode/
epicycle/.mcpjam/
epicycle/.mux/
epicycle/.neovate/
epicycle/.opencode/
epicycle/.openhands/
epicycle/.pi/
epicycle/.pochi/
epicycle/.qoder/
epicycle/.qwen/
epicycle/.roo/
epicycle/.trae/
epicycle/.windsurf/
epicycle/.zencoder/
epicycle/skills/

# --- build / render output ---
out/
build/
dist/
FB-output/

# --- _review/ = staging ไฟล์ให้พี่ปีเตอร์ฟัง/ดูทดสอบ (throwaway · ไม่ commit) ---
_review/

# --- clips/ = แล็บผลิต BGM (โน้ต/PDF → Audiveris OMR → MIDI → fluidsynth+soundfont → mp3/wav) ---
# track เฉพาะ source: scripts (.py/.ps1) + MIDI + README · กันไฟล์เสียง/soundfont/OMR intermediate (regenerable หรืออยู่ Drive _BGM-lab)
clips/*
!clips/*.py
!clips/*.ps1
!clips/*.bat
!clips/*.mid
!clips/README.md

# --- rendered media (final clips เก็บใน Google Drive horatad-media แล้ว ไม่ต้องใส่ repo) ---
*.mp4
*.mov
*.webm
*.mkv

# --- installers ---
*.exe
*.msi

# --- bundled 3rd-party tools (reinstallable binaries + Java runtime, ไม่ใช่ source) ---
tools/AudiverisExtract/
tools/fluidsynth/
*.jar

# --- logs / temp / machine-local ---
*.log
.claude/settings.local.json
Thumbs.db
desktop.ini
.DS_Store
~$*
